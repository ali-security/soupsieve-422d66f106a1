name: deploy

on:
  workflow_dispatch:

jobs:

  documents:
    if: false
    timeout-minutes: 15
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip setuptools
        python -m pip install -r requirements/docs.txt
    - name: Deploy documents
      run: |
        git config user.name ${{ secrets.GH_USER }}
        git config user.email "${{ secrets.GH_EMAIL }}"
        git remote add gh-token "https://${{ secrets.GH_TOKEN }}@github.com/facelessuser/soupsieve.git"
        git fetch gh-token && git fetch gh-token gh-pages:gh-pages
        python -m mkdocs gh-deploy -v --clean --remote-name gh-token
        git push gh-token gh-pages

  pypi:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: 3.11
      - name: Install build backend
        run: |
          python -m pip install --upgrade build "hatchling==1.14.0"
          python -c "import site; from pathlib import Path; Path(site.getsitepackages()[0], 'sitecustomize.py').write_text('import importlib.util\n')"
      - name: Package
        run: python -m build --no-isolation -s -w
      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: soupsieve-distributions
          path: dist/*
          if-no-files-found: error
